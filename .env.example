# Example environment file for docker-compose (optional)
# Copy this to .env if you want to customize docker-compose behavior
# Most users should use the one-line launchers instead (run-copilot.ps1, etc.)

# Optional: Override container name prefix
# CONTAINER_PREFIX=my-coding-agent

# Optional: Set resource limits (defaults shown)
# MAX_CPUS=4
# MAX_MEMORY=8g

# Note: Repository path and username are auto-detected
# Note: Timezone is inherited from host automatically

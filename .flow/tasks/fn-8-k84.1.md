# fn-8-k84.1 Update README with Mac support, prerequisites, and security note

## Description
Update `agent-sandbox/README.md` to indicate that `cai import` is supported on macOS with appropriate prerequisites and security notes.

**Size:** S
**Files:** agent-sandbox/README.md

## Approach

1. Change line 54 from "**Linux/WSL only** - macOS is not yet supported" to indicate Mac is supported
2. Add prerequisites section mentioning: Docker Desktop, jq, python3 (for config parsing)
3. Add note about Docker Desktop file-sharing requirements for `$HOME`
4. Add security note listing credential paths synced (host → volume):
   - `~/.claude/.credentials.json` → `/data/claude/credentials.json`
   - `~/.codex/auth.json` → `/data/codex/auth.json`
   - `~/.gemini/oauth_creds.json` → `/data/gemini/oauth_creds.json`
   - `~/.config/gh/` → `/data/config/gh/`
5. Document how to reset/remove the data volume (`docker volume rm <name>`)

## Acceptance
- [ ] README.md line 54 updated to indicate Mac support
- [ ] Prerequisites documented (Docker Desktop, jq, python3)
- [ ] Docker Desktop file-sharing note for macOS
- [ ] Security note listing specific credential paths (host → volume format)
- [ ] Volume reset/removal instructions
- [ ] Wording is clear and consistent with rest of README

## Done summary
Updated README to document Mac support for cai import with prerequisites (Docker Desktop, jq, python3), Docker Desktop file-sharing note, credential paths table showing what gets synced to the volume, and volume reset/removal instructions.
## Evidence
- Commits: 7424901, fcd02e4
- Tests: manual README review
- PRs:
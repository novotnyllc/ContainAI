# fn-8-k84.2 Add Mac-specific integration test for cai import

## Description
Add an integration test that verifies `cai import` works on macOS. The test should:
1. Detect if running on macOS (skip gracefully on Linux/WSL)
2. Run `cai import --dry-run` and verify no errors
3. Optionally verify that the expected source paths exist on macOS

**Size:** S
**Files:** agent-sandbox/tests/test-import-mac.sh (new)

## Approach

- Follow existing test patterns in `agent-sandbox/tests/`
- Use `_cai_is_macos()` from lib/platform.sh for detection
- Use `--dry-run` mode to avoid side effects
- Test should be runnable standalone and in CI

## Key context

- lib/platform.sh:85 has `_cai_is_macos()` helper
- import.sh:299 supports `DRY_RUN` mode via second argument
- Existing tests in tests/ directory for reference patterns
## Acceptance
- [ ] New test file agent-sandbox/tests/test-import-mac.sh created
- [ ] Test skips gracefully on non-macOS platforms
- [ ] Test runs `cai import --dry-run` successfully on macOS
- [ ] Test is executable (chmod +x)
- [ ] Test follows existing test patterns in the tests/ directory
## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:

# Native binaries directory
# acp-proxy - ACP terminating proxy for editor integration
#
# Build instructions:
#   cd src/acp-proxy
#   dotnet publish -c Release -r linux-x64 --self-contained
#   cp bin/Release/net10.0/linux-x64/publish/acp-proxy ../bin/

# Multi-agent profile
# Copy to: ~/.config/containai/config.toml

[agent]
default = "codex"
data_volume = "multi-agent"

[credentials]
mode = "copy"

[env]
from_host = true
import = [
  "ANTHROPIC_API_KEY",
  "OPENAI_API_KEY",
  "GEMINI_API_KEY",
]

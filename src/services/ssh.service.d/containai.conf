# ContainAI drop-in for ssh.service
# Adds container awareness and optional init service dependency

[Unit]
# Only start in container environments
ConditionVirtualization=container
# Wait for initialization to complete (if available - only in full image)
# Wants= is advisory, won't fail if unit doesn't exist
Wants=containai-init.service
After=containai-init.service

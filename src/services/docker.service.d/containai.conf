# ContainAI drop-in for docker.service
# Adds container awareness, init dependency, and sysbox ordering

[Unit]
# Only start in container environments
ConditionVirtualization=container
# Wait for initialization to complete - Requires= makes Docker fail if init fails
# This ensures startup hook failures prevent the container from being usable
Requires=containai-init.service
After=containai-init.service
